# GitHub Webhook Server Configuration

# Port for webhook server
WEBHOOK_PORT=3001

# GitHub Webhook Secret (set this to your GitHub webhook secret)
# Find this in GitHub Repo Settings > Webhooks > Secret
GITHUB_SECRET=your-secret-here

# Path to project directory containing docker-compose.yml
PROJECT_DIR=../
